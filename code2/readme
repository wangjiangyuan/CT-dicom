code2中的代码主要是针对使用
主要在于organ_based_mask_left_eye主要是：
1、先根据最高点，以最高点为中心，切割为左右各100，上方有30，下方有250。200x280的图形。基本可以把整个头部区域包裹进去。
2、先切割30-118左眼位置，通过预测，得到左眼具体的z坐标，同时可以进行调整。上下左右的。根据mask是否在边界判断。
3、将调整之后得到的左眼作为其他器官位置的标准，我有做的excel，统计了各个器官之间的位置信息，做了相关性分析。excel同时在这个文件夹中。
4、具体的更改的数值我也是经过统计的，应该差的不多
5、可能的问题，测试时更新了一些软件包，可能会有一些错误发生。mandible的bug我改了，我把shape改了一下，能够正常显示了
6、前段时间计算分子动力学，需要比较大的空间，我把mask和CT图像的npy文件删掉了，如果需要的话可能要重新写入，代码在code1中。
7、这个方法我测试过一个患者，效果还不错，但是时间比较慢，代码冗余比较多。能不能作为最终使用你再测试测试。
8、model和model.dcm是针对写入dcm的model文件，其中predict2rs_new_fix_bug这个文件会用到，我将医院给的rs-dcm文件的信息都清空之后的具有格式的空dcm文件，测试可以使用。也能打开